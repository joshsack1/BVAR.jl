CLAUDE.md
adversarial-review/

# A library's manifest is not committed: it would pin the dev environment for
# everyone who clones, and it would make CI test one frozen dependency set
# instead of the range [compat] claims to support. benchmark/Manifest.toml is
# deliberately still tracked — that is a script environment, not the package.
/Manifest.toml

# Documenter
docs/build/
docs/Manifest.toml

# local package-comparison write-up (not part of the package)
BVAR-Pkg-Comparison-2026-08-13.qmd
BVAR-Pkg-Comparison-2026-08-13.html
BVAR-Pkg-Comparison-2026-08-13_files/
BVAR-Pkg-Comparison-2026-08-13-figs/
